# Lessons Learned

Format : `[YYYY-MM-DD] | ce qui s'est mal passé | règle à suivre`

<!-- Les entrées les plus récentes en premier -->

[2026-07-17] | Franck signale « tagger une activité en Course force l'intensité en Tempo ». Je trouve la cause (`WORKOUT_INTENSITY_FLOOR.course = 'endurance_active'`) et je conclus « c'est volontaire, pas un bug » — parce que le code était manifestement délibéré (constante nommée, structure dédiée, 4 types listés). Franck reprend : c'est bien un bug, une course doit garder son intensité mesurée (un ultra se court en endurance). J'avais confondu « un dev a voulu ça un jour » avec « le comportement est correct aujourd'hui ». Corollaire trouvé en chemin : 3 tests de `classification.test.ts` échouaient depuis le durcissement des seuils de la cascade (code 10/15/20 vs tests 15/20/40) — un rouge permanent qui masquait les vraies régressions. | Face à un comportement signalé par Franck, ne JAMAIS le qualifier de « volontaire / pas un bug » sur la seule foi de l'intentionnalité du code : l'intention du code ≠ l'intention produit, et une constante bien nommée ne prouve que la première. Décrire le mécanisme, dire s'il est persisté ou dérivé, puis demander si l'intention produit correspond — c'est Franck qui tranche. Vérifier aussi qu'un échec de test voisin est bien pré-existant AVANT de l'imputer à son propre diff (`git show HEAD:<fichier>` sur les constantes, pas un `stash` risqué en sessions concurrentes). Et quand on aligne des tests sur le code : les tests de frontière (« seuil exact à X% ») doivent être RECALÉS sur les nouveaux seuils, pas juste voir leur attente inversée — sinon ils passent sans plus rien garder.

[2026-06-25] | Export « Profil de course » : les puces ravito / pastilles d'altitude / badges de montée étaient des `<div>` HTML en `position:absolute` superposés au SVG du profil. Rendu écran et export image OK, mais à l'**impression PDF** (Ctrl+P) ils se détachaient et tombaient sous le profil, parfois sur une 2ᵉ page (UTMB ~17 pts). J'ai d'abord cru à `transform:scale` sur la carte (ancêtre transformé) et l'ai retiré (dimensionnement par largeur mm) → **insuffisant, ça décrochait encore**. Le vrai problème : les overlays HTML absolus ne survivent pas à la pagination/au moteur d'impression. Fix définitif = tout dessiner **dans le SVG** (`<rect>`/`<text>`), SVG en échelle **uniforme** (`width:100%;height:auto`, retirer `preserveAspectRatio="none"` qui déformait le texte). | Pour un composant destiné à l'**impression** (PDF/Ctrl+P) : ne JAMAIS poser d'éléments HTML `position:absolute` par-dessus un graphe — ils décrochent à l'impression (pagination + containing-block). Dessiner labels/puces/badges **dans le SVG** (`<rect>`+`<text>`), comme le fait déjà `ElevationProfileChart.renderMarker`. Si le SVG est en `preserveAspectRatio="none"` (étiré), son texte est déformé → passer en échelle uniforme (`width:100%;height:auto`, viewBox au bon ratio) et réserver une bande dans le viewBox pour les overlays. ⚠️ Le rendu **print** n'est PAS vérifiable en headless ici (ni `pdftoppm`, ni Python/PyMuPDF, ni puppeteer ; la visionneuse PDF de Chrome headless ne se capture pas ; un bug print-media-only est invisible en screenshot écran) → le dire explicitement et faire confirmer par Franck au Ctrl+P.

[2026-06-22] | Import GPX manuel (Option B) cassé en prod : « GPX inexploitable. » (422). Le parser/resample marchaient (vérifié en rejouant le vrai GPX TDS : 5875 pts OK). Le throw venait de `upsertRaceTrack` : `distance_m` est une colonne **integer** et la route y insérait la distance GPX brute en **float** (`144911.93`). PostgREST (`json_populate_record`) **rejette** un float pour un integer (`invalid input syntax for type integer`) — il n'arrondit PAS. Le test unitaire **mockait Supabase** (jamais touché une vraie colonne integer) et la revue finale avait supposé « PG arrondit au cast » (faux pour ce chemin) → bug passé entre les mailles. Le float venait d'un `Math.round(dist)` retiré du parser pour satisfaire un `toBe()` de test. | Quand une valeur calculée part dans une colonne DB **typée** (surtout `integer`), ne JAMAIS se fier à un test à DB mockée ni supposer une coercition implicite : **arrondir/valider au passage vers la DB** pour matcher le type de la colonne (`Math.round` au boundary du `upsert`, pas dans la fonction pure dont un test impose la précision). PostgREST `json_populate_record` utilise la fonction d'input TEXTE → `'144911.93'::integer` = ERREUR (alors que `144911.93::numeric::integer` arrondirait). Vérifier la coercition réelle, read-only, via le MCP Supabase : `SELECT * FROM json_populate_record(null::<table>, '{"col": <val>}'::json)`. Et tester l'écriture avec un mock qui **capture le payload** envoyé à `.upsert()` et assert le type (`Number.isInteger`), pas seulement la valeur de retour.

[2026-06-21] | Retour haptique de nav (`navigator.vibrate`) « ne marchait pas » : j'ai d'abord soupçonné le code puis bumpé la durée 10→30 ms (10 ms est effectivement imperceptible sur moteurs LRA, fix valable) — mais la vraie cause était le **réglage système Android « retour haptique / intensité de vibration » à 0**. Une fois remonté, ça marche. | `navigator.vibrate()` web délègue au service `Vibrator` d'Android, qui applique l'intensité globale de l'utilisateur AVANT le moteur : intensité à 0 = amplitude nulle même avec un code correct (et le booléen retourné reste souvent `true` → l'app ne peut PAS détecter l'état). Quand une vibration web « ne se sent pas » : (1) iOS = no-op normal (API absente, aucun réglage ne l'active) ; (2) sur Android, faire vérifier le **réglage système d'intensité haptique** AVANT de toucher le code ; (3) durée ≥ ~20–30 ms (sous 20 ms les LRA n'ont pas le temps de monter en régime) ; (4) ne jamais promettre que « tous » sentiront la vibration — elle est conditionnée par un réglage hors de portée de l'app, donc bonus sensoriel jamais critique.

[2026-06-20] | Fix intensité liste↔détail (persistance `computed_intensity`) annoncé « cohérent partout », mais le Cockpit (LastActivityBlock → ActivityCard) affichait encore l'ancienne valeur. Cause : la colonne `computed_intensity` n'avait été ajoutée qu'à 2 des **5 selects** qui alimentent une `ActivityRow` ; il en restait 3 (les 2 constantes `ACTIVITY_CARD_FIELDS` de dashboard/page.tsx + plan/page.tsx, et l'inline de courses/page.tsx). | Quand une colonne devient lue par `ActivityCard`/`ActivityRow`, la liste des champs est **dupliquée dans ~5 endroits** (pas de constante partagée unique). Avant d'annoncer « partout » : `grep` le motif distinctif `manual_moving_time_sec, manual_elevation_gain_m` pour trouver TOUS les selects ActivityRow et les mettre à jour ensemble, et tracer chaque surface qui rend `ActivityCard` (liste Activités, Cockpit LastActivityBlock, Plan, Courses, recherche). Le détail public lit le stream live → pas concerné.

[2026-06-12] | Régression de contraste en **thème clair** : au tour précédent j'avais rendu le bouton « Personnaliser les colonnes » visible en sombre avec `color:#fff` codé en dur → texte blanc sur fond clair = invisible en thème clair. | Sur ce projet bi-thème (sombre par défaut + `.light`), NE JAMAIS coder une couleur de texte/fond en dur (`#fff`, `#000`) sur de l'UI : toujours des tokens thème-aware (`var(--trail-text)`, `var(--trail-primary)`, etc.). Pour un bouton secondaire qui doit ressortir sur les 2 thèmes sans concurrencer un bouton plein : **contour** (`border+color: var(--trail-primary)`, fond transparent). Vérifier mentalement le rendu dans LES DEUX thèmes après tout changement de couleur. Idem `outline` de focus : `var(--trail-text)`, pas `#fff`.

[2026-06-12] | `git worktree remove --force` sur un worktree contenant une **jonction Windows** `web/node_modules` → la suppression récursive a TRAVERSÉ la jonction et vidé le VRAI `web/node_modules` du repo principal (584 paquets effacés, jest/tsc cassés pour toutes les sessions). Récupéré par `npm ci`. | Avant `git worktree remove` d'un worktree avec jonction : **supprimer la jonction d'abord** (`cmd //c "rmdir C:\\chemin\\du\\worktree\\web\\node_modules"` — rmdir sur une jonction retire le lien SANS toucher la cible), puis `git worktree remove`. Ne jamais utiliser `--force` pour « passer outre » sans comprendre pourquoi git refuse : ici le refus initial signalait précisément le contenu non suivi (la jonction).

[2026-06-12] | 2e vague de retours prod sur l'export tableau : (1) j'avais implémenté « Annuler » comme un undo LIFO (dernière action) alors que Franck attendait l'**annulation complète du mode édition** — dans une UI mobile avec paire « Annuler / Terminé », Annuler = cancel de TOUT le mode (retour à l'état d'entrée + sortie), pas un undo incrémental ; (2) la capture JPEG basculait l'aperçu portrait→paysage→portrait (flash visible) car je dé-rotais l'élément AFFICHÉ avant rasterisation ; (3) `?export=jpeg` téléchargeait directement court-circuitant la personnalisation des colonnes. | (1) Paire Annuler/Terminé = sémantique modale : snapshot à l'entrée du mode, Annuler restaure le snapshot et ferme. (2) Pour rasteriser un élément avec un état visuel différent de l'affichage (rotation, etc.) : **cloner le node hors écran** (`wrapper position:fixed;left:-10000px` + classe modifiant le clone) et capturer le clone — ne JAMAIS muter l'élément visible. (3) Un deep-link d'export doit atterrir sur l'aperçu avec l'action mise en avant (focus), pas exécuter l'action — l'utilisateur veut vérifier/configurer avant.

[2026-06-12] | Récidive de la leçon 2026-06-08 : commit parti sur `feat/mode-mission-v2` (branche d'une AUTRE session active) au lieu de ma branche `fix/tableau-export-ux`. J'avais bien mis `git rev-parse --abbrev-ref HEAD` dans la commande de commit… mais chaîné avec `&&` : la vérif a IMPRIMÉ la mauvaise branche et le commit est parti quand même. En sens inverse, un commit de l'autre session (`b25c3bb3`) s'est égaré sur ma branche. Récupération : worktree isolé + cherry-pick sur master (zéro checkout dans le dir partagé). | Vérifier la branche n'est utile que si c'est un **gate bloquant** : `[ "$(git rev-parse --abbrev-ref HEAD)" = "ma-branche" ] && git commit … || echo "ABORT: mauvaise branche"`. Mieux : dès qu'une session concurrente est plausible (commits récents d'une autre feature dans `git log`), passer en **worktree isolé immédiatement**, pas après l'accident. Pour réparer sans toucher au working dir partagé : `git worktree add <tmp> master` + cherry-pick dedans (master peut y être checkout si aucune autre session ne l'a), puis `git worktree remove`.

[2026-06-12] | Livraison « export tableau de course » : 3 ratés UX détectés par Franck en prod sur téléphone. (1) Toolbar /print : 4 boutons en ligne dans un conteneur `width:120mm` (~454px) > écran mobile (~390px) → boutons empilés/illisibles. (2) Menu kebab `bg-trail-surface` (ink-800) sur fond ink-900 : différence de 5 % de luminosité = invisible. (3) Snackbar d'undo en bas : Franck préfère un bouton « Annuler » persistant à côté de « Terminé ». | (1) Toute barre d'actions se conçoit **mobile-first** : vérifier le rendu à ~390px (l'app est une PWA téléphone d'abord) ; jamais de largeur physique (mm) pour de l'UI écran. (2) Sur le thème sombre Deep Mission, un popover/menu doit utiliser une **surface élevée** (`--trail-card`/ink-700 minimum) + bordure `--ink-500` + ombre forte + **scrim** (`bg-black/50`) — surface-sur-bg ne contraste pas. (3) Pour l'undo d'une action destructive dans un mode d'édition, Franck préfère un **bouton persistant dans la barre du mode** (pas de snackbar transient).

[2026-06-11] | Recherche auto de course (gpt-4o-search-preview) : marche pour Saint-Jacques (UTMB) mais « introuvable » pour l'Ultra Marin (LiveTrail). J'allais tuner le prompt à l'aveugle. | NE PAS deviner sur un pipeline multi-composants LLM. (1) Isoler la couche déterministe d'abord : j'ai testé `resolveCandidates` sur la VRAIE donnée livetrail (`parcours.php`) → OK (Grand Raid 177,17 km / 1431) → donc le bug est en amont (recherche). (2) Pour voir ce que le LLM renvoie réellement en prod sans deviner : **logs runtime Vercel via le MCP** (`get_runtime_logs`, projectId+orgId dans `web/.vercel/project.json`) — `200` = pas d'erreur, et sondage par mot-clé (`livetrail`, `ultramarin-breizhchrono`) = la recherche renvoyait `ultra-marin.fr` mais JAMAIS l'URL livetrail. (3) Root cause double : recall de la recherche (LLM renvoie le site officiel, pas l'URL de chronométrage profonde) + `fetchRaceHtml` capait à 1 Mo alors que les sites officiels font >1 Mo. Fix : suivre les liens du site officiel (`/ledirect`, `/résultats` linkent l'URL livetrail) + cap 3 Mo. La clé OpenAI locale est `=test` (placeholder) → pas de repro locale ; `vercel env pull` exige un login device interactif.

[2026-06-10] | Import tableau de course : D- absent. J'ai corrigé le **parser** livetrail (dérivation du D- depuis le D+/altitude — le XML `parcours.php` n'a pas de colonne D-) et annoncé à Franck que le D- serait visible après re-import. Réponse : « il n'y a toujours pas le D- ». Cause : la donnée était bien parsée ET persistée (`PUT /api/races/[id]/waypoints` écrit `d_moins`), mais le composant d'affichage `WaypointsTable` n'avait **pas de colonne D-** (grille 6 colonnes). J'avais réparé une couche (parser) sans tracer la chaîne jusqu'au rendu. | Quand on « rend une donnée disponible/importée », tracer TOUTE la chaîne jusqu'au pixel avant d'annoncer que l'utilisateur la verra : source → parser → endpoint d'écriture → colonne DB → endpoint de lecture → mapping → **composant → colonne réellement rendue**. Un champ persisté ≠ un champ affiché. Bonus : avant de re-router un import vers un parser déterministe (URL livetrail) plutôt que le fallback IA/OCR, vérifier que les colonnes visées existent côté UI.

[2026-06-10] | Tableau de course livré en prod, puis erreur à l'import : `Could not find the 'supplies' column of 'race_waypoints' in the schema cache`. J'ai d'abord conclu « cache PostgREST périmé » (vrai symptôme classique) ET Franck avait dit « la migration est appliquée » — mais en vérifiant `information_schema.columns` via le MCP Supabase, AUCUNE des 5 colonnes de la migration 035 n'existait : elle n'avait jamais été réellement appliquée. Le déploiement du code dépendant d'une migration non appliquée a cassé la feature en prod. | Quand une feature dépend d'une migration Supabase : (1) NE PAS se fier à « la migration est appliquée » (ni la mienne ni celle de l'utilisateur) — vérifier l'existence réelle des colonnes (`select … from information_schema.columns` via le MCP) AVANT de déployer le code qui en dépend, et après tout doute ; (2) une erreur PostgREST « column not found in schema cache » a DEUX causes possibles — colonne réellement absente (migration non appliquée) OU cache périmé (DDL faite mais pas de `notify pgrst,'reload schema'`) — toujours distinguer en interrogeant `information_schema` avant de supposer le cache ; (3) après application via MCP `apply_migration`, lancer aussi `notify pgrst, 'reload schema'` par sécurité. Idéalement : appliquer/vérifier la migration AVANT le push du code, pas après.

[2026-06-10] | Maquette PDF « carte de course » (`Prompts/tableau-course-pdf-mockup.html`) : carte à hauteur fixe (65mm) qui débordait, 2 lignes coupées. J'ai « corrigé » **deux fois à l'aveugle** (réduit padding/police, calculé une marge théorique) et annoncé que ça rentrait — c'était faux les deux fois (Franck : « il n'y a pas l'arrivée »). Cause racine trouvée seulement en **mesurant** : `tbody td { line-height:1 }` sans `font-size` → le `td` hérite des 16px par défaut → strut de **16px par ligne** (lignes à 18,3px au lieu de ~11). Fix : `line-height` en **valeur absolue** (`10px`) + `font-size:9px`. | Ne JAMAIS affirmer qu'une maquette « rentre / est corrigée » sur un calcul théorique : **rendre et mesurer**. Sur Windows, Chrome installé fait du headless sans rien installer : `"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu --no-sandbox --virtual-time-budget=5000 --screenshot=OUT --window-size=W,H "file:///…"` (⚠️ écrire le PNG dans `%TEMP%`, pas dans `Prompts/` → « Accès refusé »). Pour la **vraie** marge d'une carte à hauteur fixe avec un enfant `flex:1` (qui s'étire et masque le slack → `scrollHeight==clientHeight` trompeur), injecter un `<script>` qui passe `card.style.height='auto'` et lit `getBoundingClientRect().height` = hauteur naturelle, puis `--dump-dom | grep` le résultat. Et piège CSS récurrent : `line-height:1` sur un élément sans `font-size` explicite = strut de 16px (taille héritée), pas de la taille du texte affiché.
[2026-06-08] | En exécution Subagent-Driven dans un working dir **partagé** avec une autre session Claude active (branche `feat/auth-otp-code`), un `git checkout` de l'autre session a déplacé le **HEAD partagé** entre deux de mes commits. Résultat : 3 de mes commits (Task 4 + docs) ont atterri sur la mauvaise branche, et ma branche + origin sont restées 3 commits en arrière. Détecté seulement via l'inspection du build Vercel (qui référençait un commit intermédiaire), pas via le push (« Everything up-to-date » trompeur). | Quand plusieurs sessions peuvent partager le même working dir : (1) vérifier `git rev-parse --abbrev-ref HEAD` IMMÉDIATEMENT avant CHAQUE commit et abandonner si ce n'est pas ma branche ; (2) préférer un **git worktree isolé** (HEAD séparé) dès qu'une activité concurrente est possible — sur Windows, `git config core.longpaths true` + jonction `web/node_modules` vers le repo principal (les node_modules gitignorés sont absents du worktree) ; (3) après un push, vérifier `git ls-remote origin <branche>` == tip local attendu, ne jamais se fier au seul « Everything up-to-date ». Récupération non destructive : les commits égarés restent des objets git → `git branch -f <ma-branche> <sha-du-vrai-tip>` puis push (fast-forward).

[2026-06-08] | Confirmation d'inscription / reset MdP cassés pour les utilisateurs Orange/Outlook : le lien magique (`/auth/confirm?token_hash=…`) porte un OTP à usage unique, et le scanner SafeLinks d'Outlook **prefetch** le lien → consomme le token avant le clic de l'utilisateur → `otp_expired` → `/login?error=confirm`. Diagnostic via logs auth Supabase (`POST /verify 200` suivi 2 s après de `403 otp_expired`). | Pour toute confirmation par email (signup, recovery, magic link), préférer un **code OTP à 6 chiffres** (`{{ .Token }}`, saisi en-app via verifyOtp) plutôt qu'un lien cliquable. Le code et le lien (`{{ .TokenHash }}`) étant le **même OTP**, garder le lien réintroduit la faille → templates **code-only**, lien retiré. Mode d'échec documenté Supabase : guide « OTP Verification Failures / email prefetching ».

[2026-06-08] | Après refonte des e-mails Supabase, le lien « Reset password » menait à `/auth/reset` affichant « Lien expiré ». J'avais câblé le mail sur le flow serveur `/auth/confirm?type=recovery` (verifyOtp → session en cookie → redirect), conforme à l'archi du projet, mais la page `/auth/reset` attendait l'événement client `PASSWORD_RECOVERY` (émis seulement par le flow implicite quand le token est dans le `#hash`). Token consommé côté serveur → URL propre → event jamais émis → timer 5 s → expiré. | Quand on relie un lien e-mail (Supabase) à une page existante, vérifier le **flow d'auth attendu par la page de destination** (event `PASSWORD_RECOVERY`/hash implicite **vs** session cookie via `/auth/confirm` token_hash) avant de supposer que ça marche. Les deux flows ne sont pas interchangeables. Fix robuste côté page : lire la session (`getUser()`) pour le flow serveur **et** garder l'écoute `PASSWORD_RECOVERY` pour le flow implicite, le timer ne déclenchant `expired` que si aucun des deux n'aboutit.

[2026-06-05] | En exécution Subagent-Driven (Brand Asset Pack), un subagent implémenteur (à qui j'avais laissé faire `git add`/`commit`) a fait un `git checkout master` puis a **poussé sur `origin/master`** (branche de déploiement Vercel) de sa propre initiative — alors que la consigne disait « ne pas changer de branche ». J'ai dû récupérer par cherry-pick sur la feature branch + `git branch -f master`. Le push était heureusement un fast-forward (aucun commit perdu) mais c'était un push non autorisé sur la branche de déploiement. | En exécution pilotée par subagents : **les subagents n'exécutent JAMAIS de git** (ni add, ni commit, ni surtout checkout/push). Ils créent/éditent les fichiers et lancent les tests ; le **contrôleur** fait tous les commits, sur la bonne branche, après avoir vérifié `git rev-parse --abbrev-ref HEAD`. Mettre cette interdiction explicite (« Do NOT run ANY git command, including push/checkout ») dès le premier prompt d'implémenteur, pas seulement à partir du 3e.

[2026-06-04] | Bug « un bloc déplacé/redimensionné devient moins large que les autres, corrigé seulement au F5 » sur le dashboard desktop. J'ai patché 4 fois de suite (strip des scale transforms dnd-kit, `animateLayoutChanges:false`, gating sur isDraggingAny, reflow forcé en useLayoutEffect) en restant sur CSS columns puis CSS Grid — à chaque fois « non toujours le même pb ». Cause racine : **CSS columns ET CSS Grid recalculent la largeur des items quand React réordonne les nœuds DOM**, c'est structurel, aucun patch de transform ne le corrige. | Pour une grille de blocs draggables (dnd-kit) qui doivent garder une largeur égale ET se tasser en masonry, utiliser des **colonnes flex explicites** (`flex-1 min-w-0` = 50% garanti structurellement), pas `columns-*` ni `grid-cols-*`. Mobile vs desktop via un hook `matchMedia` (init `false` pour matcher le SSR). Drag feedback via DragOverlay, zéro transform par bloc. Et arrêter de re-patcher la même approche quand l'utilisateur répète « même pb » 2× : c'est le signal de changer d'approche, pas d'ajouter un patch.

[2026-05-29] | Vercel build cassé en prod avec `Type 'MapIterator<X>' can only be iterated through when using the '--downlevelIteration' flag or with a '--target' of 'es2015' or higher` sur un `[...byId.values()]`. Le tsconfig du projet cible un downlevel qui n'accepte pas le spread d'un MapIterator. En plus, mon `npm run build` local n'avait sorti aucun output (sandbox Windows muette sur les long-running) donc je n'ai pas vu l'erreur avant le push. | Sur ce projet, ne JAMAIS spread un MapIterator (`[...map.values()]`, `[...set]`, `[...map.entries()]`) — utiliser `Array.from(map.values())`. Pareil pour `for...of` sur un MapIterator. Et quand `npm run build` local n'imprime rien (sandbox Windows), considérer la vérif comme NON-faite : pousser sur une branche de feature et attendre le retour Vercel **avant** de merger sur master, ou demander à Franck de lancer le build de son côté.

[2026-05-17] | Bug « les mini blocs de la bibliothèque Plan partent dans le calendrier semaine au scroll » : avec `PointerSensor { distance: 12 }` (seul sensor, partagé desktop+touch), n'importe quel scroll vertical sur mobile traverse 12 px immédiatement → drag activé par erreur → la séance atterrit dans le calendrier au lieu de scroller la page. La lesson 2026-05-15 m'avait fait sur-condamner `{ delay, tolerance }` ; en réalité ce qui était bugué c'était `tolerance: 8` (trop bas, geste naturel dépasse en 250 ms → cancel silencieux). Avec `tolerance: 30` le bug ne se reproduit pas. | Pour les DnD touch où l'élément est dans une page scrollable, séparer `MouseSensor { distance: 5 }` (desktop) et `TouchSensor { delay: 250, tolerance: 30 }` (mobile : long-press 250 ms, annule si le doigt bouge > 30 px = scroll). `PointerSensor` unique est piégeux car desktop et touch ont besoin de constraints différentes. La nuance vs lesson 2026-05-15 : `{ delay, tolerance }` est OK tant que `tolerance >= ~30`, c'est `tolerance` trop basse qui crée le cancel silencieux.

[2026-05-15] | Bug « impossible de drag les blocs en touch » sur /charge ET /cockpit : j'ai d'abord cherché une différence cockpit↔charge sur la foi de la perception de Franck (« ça marche mieux sur cockpit »), alors que les logs ont montré que les deux pages avaient strictement le même bug. La cause : `activationConstraint: { delay: 250, tolerance: 8 }` du PointerSensor de dnd-kit. Quand le doigt bouge > 8 px pendant les 250 ms de delay (ce qui arrive systématiquement sur un geste touch naturel), dnd-kit abandonne l'activation **silencieusement** — aucun callback `onDragCancel`, juste rien. La poignée semble morte. | Sur touch, toujours préférer `activationConstraint: { distance: N }` à `{ delay, tolerance }`. Et quand l'utilisateur dit « ça marche mieux sur X », instrumenter X **aussi** avec les mêmes logs pour valider l'asymétrie supposée avant de chercher la différence dans le code — la perception subjective ne suffit pas.

[2026-05-15] | Bug « Maximum update depth exceeded » dans BlockGrid déclenché après le premier drag réussi : un default arg `defaultHidden = []` dans la signature de la fonction crée une **nouvelle référence à chaque render**. Mis dans les deps d'un `useEffect` qui fait `setState` quand le localStorage diffère du défaut, ça forme une boucle infinie qui ne se déclenche que quand un autre `setState` (ici le `setOrder` du `handleDragEnd`) force un re-render. Bug latent pendant des mois car personne ne pouvait drag sur touch. | Tout default arg de la forme `= []` / `= {}` / fonction inline crée une nouvelle référence à chaque render ; ne JAMAIS le mettre directement en deps de `useEffect`. Soit extraire en `const` top-level, soit faire un mount-only effect avec `[]` et `// eslint-disable-next-line react-hooks/exhaustive-deps`. Vaut aussi pour les props array/object littéraux passés depuis le parent (`<Comp items={[...]}/>`).

[2026-05-15] | Bug « BottomNav non-tappable après drag-and-drop sur /charge » : j'ai cherché pendant plusieurs commits côté dnd-kit (cleanup styles, dropAnimation, PullToRefresh conflict) alors que la cause racine était dans `BlockGrid.useEffect` au mount. Le diag décisif a été quand Franck a clear son `localStorage.charge_block_order` : sans ordre custom, plus de bug. Le `setOrder()` synchrone au mount déclenche un re-render des 12 blocs (+5 charts Recharts) qui sature la file React, bloquant les transitions du router Next.js (`router.push` muet, `location.href` OK, pas de fetch pending). | Quand le router Next.js gèle après une interaction sur une page lourde, suspect immédiat = un `setState` urgent qui déclenche un gros re-render. Wrap dans `startTransition()` pour que React puisse l'interrompre. Vérifier aussi avec `localStorage.removeItem(...)` + reload pour isoler si l'état persisté est responsable.

[2026-05-14] | J'ai d'abord blâmé l'InstallPrompt (qui interceptait visuellement la BottomNav en Playwright) alors que le vrai bug chez Franck était un Service Worker servant des chunks JS obsolètes après un déploiement — `router.push()` muet, pas d'erreur, juste un router gelé. Le SW `VERSION` était hardcodé `'v2'` donc le cache ne s'invalidait jamais. | Quand un bug Next.js App Router se manifeste par « `router.push` ne navigue pas, `location.href` fonctionne, aucune erreur JS », tester EN PREMIER le clear site data (DevTools → Application → Storage → Clear). Si ça corrige → SW cache stale. La VERSION du SW doit être dérivée du SHA du commit, jamais hardcodée. Source de vérité : `web/scripts/sw.template.js` + `scripts/generate-sw.js` (auto-injecté par `scripts/build.js`).

